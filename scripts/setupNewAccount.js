var TokenIO = artifacts.require("./TokenIO.sol");


/**
 * NOTE: This script sets up a new account
 *  [ ] newAccount() ? This should be called by the client;
 *  [ ] approveAccount()
 *  [ ]
 */

module.exports = async (done) => {
  // const token = await TokenIO.deployed()
  // const receipt = await token.newAccount()

  done()
}
